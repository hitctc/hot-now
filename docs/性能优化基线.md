# 性能优化基线

> 本文是性能与结构治理的历史记录：保留当时的基线、决策、验证和部署证据，不作为当前路由、文件行数或开发规范的唯一事实源。当前模块边界见 [开发与模块化规范](./开发与模块化规范.md)，协作规则见 `AGENTS.md`，运行入口见 `README.md`。

## 目标与范围

本轮优先覆盖 AI 新讯、AI 热点、公众号素材、短内容素材、成品文章、短内容成品、文章详情、写作队列状态及数据来源工作台。旧报告、历史页和控制台不进入第一轮。

验收目标：

- 常用列表首屏在 `1` 秒内显示数据。
- 翻页、筛选和刷新在 `800` 毫秒内完成。
- 文章详情在 `1` 秒内打开。
- 普通操作在 `300` 毫秒内给出明确反馈。
- 关键接口 P95 相对基线至少下降 `50%`。
- 不减少数据、不破坏跨项目接口兼容、不新增测试失败。

## 2026-07-31 生产基线

测试方式：从生产服务器本机连续请求五次，记录 Fastify 接口总耗时和响应字节数。数据库共 `7896` 条创作素材、`1216` 篇成品文章，SQLite 文件约 `282MB`。

| 接口 | 每页 | 耗时范围 | 响应大小 |
| --- | ---: | ---: | ---: |
| 公众号素材 | 50 | 916.8～1126.6ms | 807685 bytes |
| 成品文章 | 50 | 122.2～162.7ms | 2521679 bytes |
| Hermes 写作队列状态 | - | 50.8～154.7ms | 519 bytes |

短内容的正确方向值为 `short_content`；早期探测误用了 `short`，对应空结果不纳入基线。

## 已确认的瓶颈证据

1. 素材列表和成品文章列表都读取完整详情字段。素材包含 `full_content`、`raw_payload_json`，成品文章包含正文、人工稿、证据包、提示词、HTML 和阶段轨迹，导致列表响应达到数百 KB 至数 MB。
2. 素材默认查询只有独立的 `direction` 和 `created_at` 索引，SQLite 需要扫描同方向数据并创建临时 B-tree 排序。
3. 成品文章排序包含冗余的 `(pinned_at IS NOT NULL)` 表达式，现有 `direction, pinned_at, created_at` 索引无法直接完成排序。
4. 草稿推送成功次数的相关子查询只有单列索引，没有 `(article_id, status)` 复合索引。

## 本地生产快照实验

实验只在 verified copy 上进行，没有修改生产库。按用户确认后的每页 `30` 条测试：

| 查询 | 优化前中位数 | 实验索引后中位数 |
| --- | ---: | ---: |
| 公众号素材 | 157.4ms | 2.8ms |
| 短内容素材 | 约 10ms（50 条） | 0.5ms |
| 成品文章 | 19.0ms | 8.5ms |
| 短内容成品 | 约 10ms（50 条） | 1.0ms |

实验索引消除了两个核心列表执行计划中的临时 B-tree 排序。下一批仍需改为轻量列表模型，继续降低 JSON 解析、序列化、网络传输和 Vue 响应式处理成本。

## 第二批实施结果

迁移 `046_creative_list_performance_indexes` 增加三条经过执行计划验证的复合索引。生产快照上的迁移耗时约 `1.5` 秒，迁移后 `user_version=46`、`quick_check=ok`。

Vue 创作列表显式请求 `view=summary`，外部 Agent 不传该参数时仍获得原完整列表响应，保持接口兼容。素材展开和文章详情改为按需读取完整记录；四个核心列表默认分页统一为 `30` 条。

在已应用正式索引的生产快照副本上，最终轻量列表结果如下：

| 查询 | 中位数 | 响应大小 | 相对原生产响应 |
| --- | ---: | ---: | ---: |
| 公众号素材 | 0.30ms | 37634 bytes | 减少约 95% |
| 短内容素材 | 0.32ms | 43383 bytes | - |
| 成品文章 | 3.43ms | 59005 bytes | 减少约 98% |
| 短内容成品 | 0.92ms | 78533 bytes | - |

写作队列状态请求在前端 service 层共享进行中的请求，并保留两秒短缓存，避免全局浮标、监控页和写作轮询在同一时刻重复访问 Hermes。

## 生产部署后复测

迁移 `46` 部署后，生产库 `quick_check=ok`，三条性能索引均已存在。服务器本机首次逐接口请求结果如下：

| 接口 | 每页 | 应用耗时 | 请求总耗时 | 响应大小 |
| --- | ---: | ---: | ---: | ---: |
| 公众号素材摘要 | 30 | 3.1ms | 66.7ms | 38438 bytes |
| 短内容素材摘要 | 30 | 1.6ms | 8.8ms | 41954 bytes |
| 成品文章摘要 | 30 | 10.9ms | 17.0ms | 70971 bytes |
| 短内容成品摘要 | 30 | 5.0ms | 13.6ms | 88280 bytes |
| 素材详情 | - | 0.7ms | 6.3ms | 2194 bytes |
| 成品详情 | - | 0.9ms | 6.8ms | 2784 bytes |

四个核心列表和两个详情接口均低于本轮目标阈值。公网 `/health` 同时返回正常，因此本轮不再增加长期缓存或额外查询层。

## 第三批结构治理

- 服务端创作素材/成品的列表、详情和来源名称只读接口集中到 `src/server/routes/creativeListRoutes.ts`，`createServer.ts` 只负责注入数据库、token 和 session 鉴权。
- 图片转存、Base64 上传、前端上传和公开图片读取集中到 `src/server/routes/creativeImageRoutes.ts`，`createServer.ts` 只负责注入图片目录、公开地址和鉴权回调。
- 前端创作记录类型及列表/详情查询集中到 `src/client/services/creativeListApi.ts`，原 `creativeApi.ts` 继续重新导出这些公开成员，现有页面不需要改导入路径。
- 本批只拆出可独立命名、可单独验证的边界；未强拆与编辑状态高度耦合的详情抽屉，也没有为了压缩行数增加碎片模块。

## 2026-07-31 列表连接阻塞复盘

浏览器曾记录成品文章第 `21` 页请求总耗时 `36.51s`，其中连接等待（Stalled）为 `36.28s`，服务端响应等待只有 `229.40ms`。相同请求在生产服务器连续执行为 `65.5～129.1ms`，20 路并发最慢约 `1.29s`，因此不是列表接口执行了 36 秒。

根因证据：

1. 截图中第 `21` 页之前访问过第 `6` 页；该页有 `17` 张同域封面，总量约 `35MB`，中位数约 `2.08MB`，全部超过 `1MB`。
2. 单张封面通过公网下载曾约 `4.3s`；生产站只启用了 HTTP/1.1，大图会占用有限的同域连接，使后续 XHR 在浏览器连接池等待。
3. 写作队列状态代理同期出现过 `6.7s`、`8.9s` 和 `10.8s` 慢请求，进一步占用连接。
4. 勾选“显示已废弃”后，迁移 `46` 的部分索引不适用，查询需要临时排序；这只贡献约 `62～65ms`，属于次要瓶颈。

本批修复：

- 本地图片增加最大边长 `96px`、质量 `72` 的 WebP 缩略图。新图片上传时生成，历史图片首次访问时生成并原子缓存；失败只显示占位，不回退下载原图。
- 成品列表只加载缩略图，增加懒加载、异步解码和低请求优先级，点击预览时才读取原图。
- 四个核心创作列表在新请求开始时取消旧请求，只有最后一次结果可以更新页面。
- 写作队列状态使用服务端单飞和 `15s` 新鲜缓存，Hermes 最多等待 `3s`；失败时返回五分钟内最近一次成功状态并显示延迟标记，页面隐藏时暂停轮询。
- 迁移 `47` 增加覆盖全部成品的方向/置顶/创建时间索引；生产快照副本迁移约 `1.06s`，`quick_check=ok`，包含已废弃文章的查询不再临时排序，中位数约 `0.021ms`。
- Nginx 模板启用 HTTP/2，并提供备份、`nginx -t`、失败回滚和 reload 的一次性脚本；生产切换必须由具备 sudo 权限的用户执行。

## 列表连接阻塞修复的生产验收

提交 `3bf00db`、`08b35af` 已部署。部署前数据库健康检查通过，并生成验证快照
`/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-07-31T09-07-28-349Z.sqlite`；迁移后
`user_version=47`、`quick_check=ok`。包含已废弃文章的查询命中新完整索引，不再使用临时排序。

原问题中的第 `21` 页成品文章摘要接口，生产服务器本机连续十次请求为 `3.4～7.3ms`，经公网
Nginx 连续五次请求为 `54～73ms`。第 `6` 页的 `17` 张历史封面首次并发生成缩略图约 `835ms`，
缓存后约 `63ms`；缩略图合计 `14666` bytes，单张最大 `1228` bytes，经公网并发读取约 `210ms`。
列表不再预加载原先约 `35MB` 的原图。

生产依赖审计为 `0` 漏洞，服务健康检查通过。HTTP/2 脚本已部署，但当前发布用户没有非交互式
root 权限，因此生产仍为 HTTP/1.1；需由具备 sudo 权限的用户执行
`sudo bash /srv/hot-now/app/scripts/enable-nginx-http2.sh` 后再复验协议。

## Phase 1 素材列表拆分的生产验收

提交 `f68e0c4` 已推送并部署。部署前生产数据库预检通过并生成快照
`/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T03-39-04-108Z.sqlite`；本批未修改数据库结构。
生产服务重启后 `/health` 返回 `{"ok":true}`，新客户端入口静态资源返回 `200`，未登录访问创作列表在约
`0.153s` 内返回 `401`，未出现连接阻塞。生产依赖安装完成后审计为 `0` 漏洞。

本批部署的是页面结构拆分，不宣称已完成文章详情或服务端大文件治理；下一批仍按地图进入 `ArticleDetailDrawer.vue`。

## 监测约定

- 服务端默认只记录不低于 `500ms` 的慢请求，可用 `HOT_NOW_SLOW_REQUEST_MS` 调整。
- 每个响应包含 `Server-Timing`，便于在浏览器开发者工具区分服务端耗时和网络／前端耗时。
- 前端开发模式只在请求超过 `500ms` 时输出脱敏路径、状态码、总耗时和服务端计时。
- 正常快速请求保持静默，日志不包含查询参数、正文、Cookie 或密钥。

## 大文件治理的本次完成标准

本次“大文件优化”不以减少行数为验收目标。验收对象是代码边界是否清晰、业务逻辑是否保持不变、后续修改是否能定位到明确模块。只有同时满足以下条件，才算完成结构治理：

1. `src/server/createServer.ts` 只负责 Fastify 实例创建、全局配置/中间件、依赖注入和路由模块注册；具体业务处理器不再继续堆积在装配入口。
2. Vue 页面文件只负责页面编排、路由参数和页面级组合；请求、状态、轮询和副作用进入 composable（组合式逻辑），筛选栏、表格、状态单元格、展开区和操作区进入有明确输入输出的组件。
3. `ArticleDetailDrawer.vue` 按完整功能和视觉区域拆分，编辑器、标题同步、保存/专注、图片提示词、图片占位符、预览、审核和发布动作可以分别阅读和验证；不以“每个按钮一个文件”制造碎片。
4. 服务端路由按创作素材/成品、日报、监控、设置和 legacy 等业务域拆分；业务域内部再按稳定的读、写、图片、发布边界拆分，不按单个 endpoint（端点）机械切片。
5. 核心 repository（仓储）和迁移入口按稳定职责拆分；迁移顺序、幂等性、历史数据和外部 API 保持不变。
6. 每批拆分前有最小行为基线，拆分后通过相关单元/集成测试、类型检查、生产构建和必要的浏览器/API 冒烟；失败可独立回滚。
7. 公开 URL、HTTP 方法、请求/响应结构、鉴权、数据库字段语义和用户操作结果不改变；发现既有失败必须单独记录，不得用重构掩盖。

### 当时的阶段状态

本节记录地图建立时的阶段判断。后续子批结果以本文后续带日期的记录为准；当前模块边界和完成情况不在这里重复维护。

## 长期开发规范

- **先定义边界，再写代码**：新增或重构前先在本文件更新所属业务域、输入/输出、依赖和验证边界；地图未更新，不开始大范围拆分。
- **页面编排与业务逻辑分离**：页面保留路由、组合和布局；API 调用、查询参数、分页、轮询、乐观更新和错误处理进入 composable 或 service（服务）层。
- **组件按用户可理解的功能切分**：组件以筛选、表格、编辑器、预览、图片、发布等完整区域为边界，props/emits（属性/事件）显式定义；禁止用一个“万能页面”堆叠布尔开关。
- **服务端按业务域组织**：路由模块拥有清晰的依赖注入；handler（处理器）只负责协议适配，领域规则进入 core（核心层），数据库读写进入 repository，不在入口文件中跨层拼接。
- **核心层按稳定职责拆分**：读模型、写入、提示词、发布和迁移分别有可测试边界；不为追求小文件把一次事务拆成互相跳转的碎片。
- **依赖显式且最小**：新模块只接收实际需要的类型化依赖，不通过大型配置对象或隐式全局状态传递上下文。
- **测试镜像模块边界**：纯函数优先单测，composable 和组件覆盖关键交互，路由/仓储保留少量跨模块集成测试；重构不得删除原有行为覆盖。
- **不做无谓格式化和抽象**：不引入与本批目标无关的 lint（代码规范）工具链、全局格式化或未来假设型配置；行数下降只能作为结果，不能作为目的。
- **文档与代码同批更新**：HotNow 结构变化同步本文件；涉及公众号写作工作台时，同时更新 Hermes PRD 与决策记录。文档和代码不一致时先以代码事实为准，再修正文档。

## 大文件拆分地图

地图按“业务域 → 稳定职责 → 可验证边界”规划。数字是当前基线，用于识别风险，不是强制的行数 KPI。

### 第 0 批：标准与地图（本批）

- 建立本节完成标准、长期开发规范和分批回滚规则。
- 记录当前大文件、优先级、候选拆分边界和不应拆分的耦合关系。
- 不改运行时代码；完成后单独提交文档，作为后续批次的审查依据。

### 第 1 批：素材列表页面（本批已完成）

目标文件：

- `src/client/pages/creative/SourceItemsPage.vue`（1308 行）
- `src/client/pages/creative/ShortSourceItemsPage.vue`（1093 行）

候选模块：

- `src/client/components/creative/source-items/SourceItemsFilterBar.vue`：筛选条件和分页入口。
- `SourceItemsTable.vue`：表格、行选择和展开槽位。
- `SourceItemStatusCell.vue`：适配、写作、失败和中止状态展示。
- `SourceItemActionMenu.vue`：评估、写作、重试和删除等操作。
- `SourceItemExpandedRow.vue`：按需读取后的素材详情和证据摘要。
- `useSourceItemsQuery.ts`：查询参数、分页、排序、取消过期请求和加载错误。
- `useSourceItemWritingPoll.ts`、`useSourceItemTracePoll.ts`：写作队列和阶段轨迹轮询。
- `sourceItemPresentation.ts`：纯展示映射、颜色和标签文案。

长文章页和短内容页继续保留独立入口；只复用稳定组件和 composable，不合并成靠大量模式开关控制的万能页面。

实施结果：

- 新增 `sourceItemPresentation.ts`，集中纯展示映射、时间格式化、评分柱状图和状态/适配度文案，并补充 4 项单元测试。
- 新增 `useSourceItemsQuery.ts`，集中筛选持久化、分页、摘要列表请求、旧请求取消、展开详情按需读取和滚动补偿；长文额外启用账号适配筛选，短内容保持关闭。
- 新增 `SourceItemsFilterBar.vue` 和 `SourceItemsTable.vue`，两条入口共享筛选栏、搜索历史、表格、展开区、溯源/写作操作事件；`mode` 只保留必要的长文/短内容差异。
- `SourceItemsPage.vue` 从 `1308` 行降至 `594` 行，`ShortSourceItemsPage.vue` 从 `1093` 行降至 `483` 行；未改变 URL、API 参数、鉴权或用户操作语义。
- 验证通过：客户端类型检查、生产客户端构建、展示模块 4 项测试、请求取消 2 项测试、写作轮询结果 2 项测试。

### 第 2 批：文章详情与编辑器（本批已完成）

目标文件：

- `src/client/components/creative/ArticleDetailDrawer.vue`（2789 行）
- `src/client/components/creative/ArticleMarkdownEditor.vue`（593 行）
- `src/client/components/creative/StepTraceTimeline.vue`（594 行）

候选模块：

- 详情头部、标题/导语/摘要区、三栏编辑器壳、左栏素材区、中栏人工转写区、右栏预览区。
- `useArticleEditorState.ts`：正文草稿、自动保存、专注模式和退出清理。
- `useArticleTitleSync.ts`：手动稿与管线稿的发布标题/首个一级标题同步规则。
- `useArticleImageWorkflow.ts`：封面提示词、正文配图提示词、编号占位符和上传替换。
- `useArticlePresentation.ts`：主题、状态标签、倒计时和只读展示映射。
- 审核/发布操作区和阶段轨迹作为独立视觉区域，不把请求细节重新塞回页面。

标题同步、图片占位符、保存和发布的现有行为必须先写最小基线，再进行组件迁移。

实施结果：

- `ArticleDetailDrawer.vue` 从 `2789` 行收口为抽屉编排、文章状态和请求协调；详情头部、底部发布操作、文章规划信息、相似度、补充信息、短内容提示词、封面/正文配图工作流和三栏编辑器壳均为独立组件，使用明确的 `props/emits`，不新增万能组件。
- 新增 `articleTitleSync.ts` 和 `articleDetailPresentation.ts`，将手动稿/管线稿标题与首个 H1 同步、历史 JSON 安全解析、状态文案和字数统计改为可独立测试的纯函数；新增 6 项对应单元测试。
- 新增 `useArticleEditorViewport.ts`，集中专注模式、动态高度、同步滚动偏好、全屏滚动位置恢复及 DOM 事件清理；正文保存、自动保存、主题渲染和推送仍保留在抽屉编排层，避免在这批拆分中改变请求时序。
- `ArticleMarkdownEditor.vue` 与 `StepTraceTimeline.vue` 已分别是明确的编辑器/阶段轨迹边界，本批不为压缩行数继续机械切分内部交互；未改变公开 URL、接口、数据库字段、图片占位符或人工转写保存语义。
- 验证通过：客户端类型检查、生产客户端构建、标题同步/详情展示/创作 API 定向测试；全量主题测试中已有“浅色页面背景色”断言与当前落日胶片令牌不一致，本批未修改主题或该既有测试。

### 第 3 批：服务端路由域（已完成）

目标文件：

- `src/server/createServer.ts`（Phase 3 启动时约 6213 行；行数仅作定位，不作验收指标）
- 已有 `src/server/routes/creativeListRoutes.ts`、`src/server/routes/creativeImageRoutes.ts` 作为边界样例。

拆分顺序：

1. 创作素材的写入与写作调度路由（已完成）；
2. 成品文章的写入、编辑、发布和提示词路由（已完成）；
3. 日报、监控和队列状态路由（已完成）；
4. 设置 API 与公众号账号管理路由（已完成）；
5. 手动采集路由（已完成）；
6. 通用来源配置路由（已完成）；
7. Twitter 账号与关键词来源配置路由（已完成）；
8. Hacker News/B站查询来源配置路由（已完成）；
9. 公众号 RSS 来源配置路由（已完成）；
10. 内容操作与站点页面路由（已完成）；
11. 复核入口只剩装配与注册，不再出现业务 SQL、文件操作和长 handler（已完成）。

每个业务域保持原 URL、鉴权回调、响应结构和错误语义；先迁移可独立验证的域，再处理跨域耦合。

#### Phase 3 中间复核（2026-08-01）

- Phase 3 中间复核时，`createServer.ts` 约 `3010` 行；行数只用于定位，不作为完成标准。后续路由收口结果见本节后的子批记录。
- 已拆的创作、日报、设置和来源模块均以独立数据模型、鉴权边界或外部服务语义为依据；来源配置的通用 RSS、Twitter、查询来源和公众号 RSS 虽然文件较小，但不应为合并而合并成万能模块。
- 后续不再以“迁移若干端点”描述或验收。剩余重构按 `contentFeedbackRoutes`、`aiTimelineRoutes`、`providerSettingsRoutes`、`sitePageRoutes` 四个完整业务域推进；每个模块必须拥有内聚的协议适配和领域边界，而非仅作为端点搬运容器。
- 每批仍以不改变 URL、鉴权、响应和数据库语义为底线，并通过定向测试、类型检查、生产构建、部署和生产验证闭环。

第 1 子批实施结果：

- 新增 `src/server/routes/creativeSourceActionRoutes.ts`，承接素材接收、长文/短内容触发、适配度评估、手动输入写作、溯源、状态回写、可写开关、趋势分和账号适配共 12 个端点。
- `createServer.ts` 只注入数据库与三类既有鉴权回调（Agent token、管理端 session、状态操作），不再承载这些路由处理器、Hermes 调用和 SQL 回写细节。
- 验证通过：服务端 TypeScript 检查，以及素材状态/账号适配、手动成品/图片提示词等既有路由回归测试；成品文章、日报、监控、设置和 legacy 路由仍留在入口，等待后续子批迁移。

第 2 子批实施结果：

- 新增 `src/server/routes/creativeFinishedArticleRoutes.ts`，承接成品文章写入、编辑、人工新建、置顶、发布状态、发布反馈、删除/恢复、微信草稿推送、推送记录、标题/导语/摘要重生、封面与正文配图提示词、短内容配图等共 24 个端点。
- 成品域只接收数据库、三类既有鉴权策略及两个微信推送回调；原 URL、HTTP 方法、token/session 状态判断、Hermes 超时、SSE 推送格式、错误码和数据库写入顺序保持不变。尤其保留旧的 session 读取 `undefined` 语义，避免在未启用登录时悄然扩大接口权限。
- 验证通过：服务端 TypeScript 检查、`manualFinishedArticlesRoute`、`creativeImageRoute`、`creativeListSummaryRoute` 共 9 项回归测试，以及完整生产构建。日报、监控、队列、设置和 legacy 路由仍留在入口，等待后续子批迁移。

第 3 子批实施结果：

- 新增 `src/server/routes/creativeDailyDigestRoutes.ts`，承接日报 Agent 写入、列表/详情、编辑或状态更新、手动生成和微信草稿推送共 6 个端点；新增 `src/server/routes/hermesOperationalRoutes.ts`，承接写作队列、Hermes 监控、Codex 任务可观测、任务立即触发和两条人工生图代理共 16 个端点。
- 运行域在模块内部创建原有写作队列读取器，保留 3 秒超时/缓存降级；日报域只接收数据库、既有 token/session/状态操作鉴权和微信日报推送回调。原 URL、HTTP 方法、session 的 `undefined` 语义、Hermes 转发路径和 query、超时、SSE 格式与数据库写入顺序不变。
- 新增 `tests/server/creativeDailyDigestRoute.test.ts`，覆盖日报 token 写入、同日期幂等覆盖和 token 状态更新；端点集合静态对照为迁移前后 22/22 一致。服务端 TypeScript、11 项定向回归测试和完整生产构建通过。设置、来源管理与 legacy 路由仍留在入口。

第 4 子批实施结果：

- 新增 `src/server/routes/settingsApiRoutes.ts`，承接视图规则、来源设置读取、个人资料与密码、AI 时间线设置别名、公众号账号管理共 12 个端点。
- 设置模块只接收显式的 session/状态操作鉴权、数据读取、密码验证/更新和公众号账号回调；不接收完整 `ServerDeps`。原 URL、HTTP 方法、session 的 `undefined` 语义、密码校验、错误码和公众号账号保存逻辑不变。
- 静态端点集合对照为迁移前后 12/12 一致；`settingsApiRoutes.test.ts` 9 项通过，服务端 TypeScript 通过。扩展的 `createServer.test.ts` 中 4 条登录重定向断言仍是既有失败：断言期望 `/login`，当前实现返回保留回跳目标的 `/login?redirect=...`；本批未修改该行为或测试。来源管理、内容操作和 legacy 页面路由仍留在入口。

第 5 子批实施结果：

- 新增 `src/server/routes/manualCollectionRoutes.ts`，承接常规采集及兼容别名、最新邮件重发、Twitter 账号/关键词、Hacker News、B 站、公众号 RSS、微博、Juya 和 AI 时间线手动采集共 11 个端点。
- 模块只转发入口层已存在的处理函数和鉴权回调；原有鉴权、运行锁、任务触发、返回状态和 AI 时间线 `410` 拒绝语义保持不变，不将来源配置管理混入同一模块。
- 静态端点集合对照为迁移前后 11/11 一致；`systemRoutes.test.ts` 29 项、服务端 TypeScript 和完整生产构建通过。下一子批按通用来源、社交/搜索来源与 RSS 来源拆分配置管理，避免形成新的万能路由模块。

第 6 子批实施结果：

- 新增 `src/server/routes/sourceManagementRoutes.ts`，承接通用 RSS 与微信桥接来源的启停、新建、更新、删除和展示模式共 5 个端点，以及仅服务该域的请求解析和响应映射。
- 模块只接收明确的状态鉴权与来源仓储写入回调；原 URL、方法、校验、HTTP 状态码和响应体保持不变。Twitter、Hacker News/B 站、公众号 RSS 的独立模型与写入规则仍留在入口，等待各自的独立模块。
- 静态端点集合对照为迁移前后 5/5 一致；`systemRoutes.test.ts` 29 项、服务端 TypeScript 和完整生产构建通过。

第 7 子批实施结果：

- 新增 `src/server/routes/twitterSourceRoutes.ts`，承接 Twitter 账号的新建、更新、删除、启停，以及关键词的新建、更新、删除、采集开关、展示开关共 9 个端点。
- 路由模块只接收明确的状态鉴权和 Twitter 仓储回调；账号/关键词各自的请求解析、去重错误映射和正整数 id 校验随模块迁移，原 URL、方法、校验、状态码和响应字段保持不变。
- 静态端点集合对照为迁移前后 9/9 一致；`systemRoutes.test.ts` 29 项、服务端 TypeScript 和完整生产构建通过。Hacker News/B站和公众号 RSS 仍保持独立边界。

### 第 4 批：核心 repository 与数据库迁移

目标文件：

- `src/core/creative/creativeFinishedArticleRepository.ts`（1123 行）
- `src/core/creative/creativeSourceItemRepository.ts`（730 行）
- `src/core/db/runMigrations.ts`（1680 行）
- `src/core/content/buildContentViewSelection.ts`（994 行）

候选边界：

- 成品/素材 repository 的列表读模型、详情读取、写入更新、图片/提示词和发布记录。
- `runMigrations.ts` 只保留版本调度、事务和失败处理；已归档的 `001`–`013` 保持独立版本文件，未归档的 `014`–`047` 保留原处；从 `048` 起新增迁移必须按序独立归档，并保留幂等和历史版本。
- 视图选择逻辑拆为纯选择器、字段映射和查询参数验证，先保证输出快照不变。

本批不得顺手改表结构、删除历史数据或调整业务规则；数据库迁移若非结构治理必需，另立需求和验证记录。

### 当时暂不优先的文件

以下是地图建立时的候选项和行数快照，不代表当前状态：`FinishedArticlesPage.vue`、`ShortFinishedArticlesPage.vue`、`useSourcesPageController.ts`、`main.ts`、`renderSystemPages.ts`、`settingsApi.ts`、`creativeApi.ts`。后续是否拆分只看稳定职责边界或真实维护痛点，不按行数继续切分。

第 8 子批实施结果：

- 新增 `src/server/routes/querySourceRoutes.ts`，承接 Hacker News 与 B站查询词的增改删和启停共 8 个端点。
- 静态端点集合对照为迁移前后 8/8 一致；`systemRoutes.test.ts` 29 项、服务端 TypeScript 和完整生产构建通过。

第 9 子批实施结果：公众号 RSS 创建、更新、删除 3 个端点迁移至 `wechatRssRoutes.ts`；静态对照 3/3，系统路由测试、类型检查和生产构建通过。

第 10 子批实施结果：`contentFeedbackRoutes.ts` 收口内容反馈、人工评分与反馈池维护 4 个端点；系统路由测试、类型检查和生产构建通过。

AI 时间线域：共享读模型 `aiTimelineApiData.ts` 供公开路由与设置页复用，`aiTimelineRoutes.ts` 承接 Feed、公开读取和只读保护；定向测试、类型检查和生产构建通过。

模型供应商密钥保存、启停和删除已并入 `settingsApiRoutes.ts`；相关测试、类型检查和生产构建通过。

第 11 子批实施结果：

- 新增 `src/server/routes/sitePageRoutes.ts`，按完整站点页面边界承接健康检查、站点/品牌/客户端静态资源、公开内容读取、创作 Feed、登录/退出、统一壳层、legacy `/history`、`/reports/:date`、`/control` 和 HTML/JSON 404。
- 路由模块只接收内容读模型、报告读取、客户端入口、认证会话、创作 token、数据库和系统页面渲染实际需要的依赖；`createServer.ts` 只保留 Fastify 创建、性能监测、最小依赖装配和各路由域注册，已不再直接注册 HTTP handler。
- 登录回跳、匿名内容页、系统/创作页保护、legacy 路由保护、静态目录越界拦截、原 URL、响应格式及 404 协议保持不变。`createServer` 与系统路由定向测试中 50 项通过；4 项既有断言仍错误地期望 `/login` 而当前既有行为是安全回跳 `/login?redirect=...`，本批未修改该行为或测试。服务端 TypeScript 检查与生产构建通过；客户端构建仍保留既有 `dailyDigestApi.ts` 同时静态/动态导入提示。

Phase 3 至此完成。下一批进入 Phase 4：按稳定读写职责拆分核心 repository、内容选择与迁移调度；不修改历史迁移、数据库语义或公开 API。

### Phase 4 第 1 子批：内容选择二级来源范围

- 新增 `src/core/content/contentViewSelectionSourceScope.ts`，承接 Twitter 账号/关键词与公众号 RSS 聚合来源的二级 ID 范围过滤及三个已有仓储读取器。
- `buildContentViewSelection.ts` 保留主查询、候选评分、去重、排序和指标编排，只调用该范围模块；筛选输入、SQL 查询顺序和输出不变。
- `npm test -- tests/content/buildContentViewSelection.test.ts` 16 项通过，服务端 TypeScript 检查通过。

### Phase 4 第 2 子批：内容选择共享类型

- 新增 `contentViewSelectionTypes.ts`，定义展示排序方式、评分卡片与内部候选卡片的共享类型；原 `buildContentViewSelection.ts` 保持原导出路径并重新导出公开类型。
- 未改变运行时代码、SQL、评分、排序或输入输出；内容选择定向测试 16 项和服务端 TypeScript 检查通过。

### Phase 4 第 3 子批：内容选择评分与排序

- 新增 `contentViewSelectionRanking.ts`，承接视图评分、来源加分、时效评分、默认排序和两种用户排序的纯函数。
- 主选择器保留查询、范围过滤、去重和指标编排；评分权重、排序优先级、时间戳兜底和公开输入输出不变。
- 内容选择定向测试 16 项与服务端 TypeScript 检查通过。

### Phase 4 第 4 子批：内容卡片展示映射

- 新增 `contentViewCardPresentation.ts`，收口 metadata 容错解析与 Twitter、公众号 RSS、B站、Hacker News 的来源详情展示。
- 新增 3 项纯函数测试；内容选择与展示映射共 19 项测试、服务端 TypeScript 检查通过，查询、筛选、评分和排序语义不变。

### Phase 4 基线修复：素材仓储测试夹具

- `creativeSourceItemRepository` 先以 `externalId + collectorAgent` 查询，再以 `url + collectorAgent` 兜底去重；数据库约束仍只有前者。
- 三个列表测试原本为不同素材复用了同一 URL，触发既有 URL 去重而错误地只保留一条记录；现改为各素材使用不同 URL。运行时代码、数据库和去重契约均未变更。
- `creativeSourceItemRepository.test.ts` 24 项测试与服务端 TypeScript 检查通过。

### Phase 4 第 5 子批：素材仓储读取边界

- 新增 `creativeSourceItemReadRepository.ts`，承接素材详情、按外部 ID 查找与列表/摘要读取；列投影、行映射、筛选、分页、24 小时模式与历史采集器回退查询保持不变。
- 新增 `creativeSourceItemTypes.ts`，收口素材记录、插入输入、列表筛选、评分、账号适配和停止说明的共享类型。
- `creativeSourceItemRepository.ts` 保留写入更新，并重新导出原读取函数和全部公开类型；调用方无需改动导入路径。素材、成品关联和路由定向测试共 50 项及服务端 TypeScript 检查通过。

### Phase 4 第 6 子批：素材仓储写入边界

- 新增 `creativeSourceItemWriteRepository.ts`，承接采集幂等插入、写作状态、趋势评分、账号适配、人工字段、成品关联与可写标记更新。
- `creativeSourceItemRepository.ts` 收窄为兼容出口，统一重新导出读取、写入和共享类型；既有采集器、路由、成品仓储和 Hermes 调用保持原导入路径。
- 未改动 SQL、幂等字段补全、写入时序、状态迁移或公开 API；素材、成品关联与路由定向测试共 50 项及服务端 TypeScript 检查通过。

### Phase 4 第 7 子批：成品文章仓储读取边界

- 新增 `creativeFinishedArticleReadRepository.ts`，承接成品详情、按素材查找及列表/摘要读取；JSON 映射、轻量投影、筛选、软删除排除和置顶排序保持不变。
- 新增 `creativeFinishedArticleTypes.ts`，收口成品记录、写入/编辑输入、列表筛选、流程追踪、标题候选与效果反馈共享类型。
- `creativeFinishedArticleRepository.ts` 保留插入、状态校验、编辑、发布反馈和状态开关，并重新导出读取函数和全部公开类型；成品、素材关联、摘要列表和手动成品路由共 52 项定向测试及服务端 TypeScript 检查通过。

### Phase 4 第 8 子批：成品文章仓储写入与发布边界

- 新增 `creativeFinishedArticleWriteRepository.ts`，承接成品新建、人工编辑、发布条件和状态转换校验、效果反馈、公众号发布/可发标记、软删除/恢复及置顶。
- `creativeFinishedArticleRepository.ts` 收窄为兼容出口，重新导出读取、写入与共享类型；路由、写作管线与微信草稿推送继续使用原导入路径。
- 未改动 SQL、素材双向关联、发布状态机、推送前置条件、效果反馈快照或写入时序；成品、素材关联与创作路由共 56 项定向测试及服务端 TypeScript 检查通过。

### Phase 4 第 9 子批：迁移 001 基线归档

- 新增 `src/core/db/migrations/001_unified_site_baseline.ts`，原样归档统一站点基线建表 SQL、版本 `1` 与迁移名称。
- `runMigrations.ts` 继续在原事务和原顺序中执行 001 语句并写入相同 `schema_migrations` 记录；后续历史迁移、`user_version`、幂等列检查与外键检查均未调整。
- `runMigrations.test.ts` 4 项及本地 `db:check` 数据库完整性检查通过。

### Phase 4 第 10 子批：迁移 002–005 增量归档

- 新增 `002_digest_report_mail_attempts.ts`、`003_feedback_and_llm_strategy_workbench.ts`、`004_source_display_mode.ts`、`005_nl_rule_enabled_flag.ts`；每个版本独立保留原名称、版本号与 SQL/列检查逻辑。
- 新增 `columnHelpers.ts` 复用既有 `PRAGMA table_info` 列检查；`runMigrations.ts` 仍按原事务和原顺序调用各版本并写入相同迁移记录。
- 未调整任何建表/加列 SQL、幂等条件、`user_version` 或外键检查；`runMigrations.test.ts` 4 项及本地 `db:check` 通过。

### Phase 4 第 11 子批：迁移 006 供应商设置表归档

- 新增 `006_provider_settings_multi_save.ts`，原样归档旧供应商设置表重命名、按厂商重建、数据复制和“单启用厂商”部分唯一索引。
- 新增旧表升级基线：构造带 `id` 的历史表，验证保存的厂商记录与索引迁移后均保留。
- 调度器仍在原事务和原顺序执行版本 6 并写入相同记录；`runMigrations.test.ts` 5 项及本地 `db:check` 通过。

### Phase 4 第 12 子批：迁移 007 来源桥接元数据归档

- 新增 `007_source_bridge_metadata.ts`，原样归档 `content_sources` 的 `source_type`、`bridge_kind` 与 `bridge_config_json` 三个幂等加列。
- 调度器仍在原事务和原顺序调用版本 7 并写入相同迁移记录；未调整默认值、后续迁移、`user_version` 或外键检查。
- `runMigrations.test.ts` 5 项及本地 `db:check` 通过。

### Phase 4 第 13 子批：迁移 008–013 外部来源配置归档

- 新增版本 `008` 至 `013` 的独立迁移文件，分别保留 Twitter 账号/关键词、Hacker News、B站、微博热搜和公众号 RSS 的配置表、索引、关联表与隐藏聚合来源写入。
- `runMigrations.ts` 仍在原事务中按 8→13 顺序调用并写入相同版本记录；未合并不同来源 SQL，也未改变字段、索引、upsert 或来源展示语义。
- 迁移、来源工作台及六类来源仓储共 33 项定向测试和本地 `db:check` 通过；本组只部署一次。

### Phase 4 决策：历史迁移归档范围收敛（2026-08-01）

- 已归档 `001` 至 `013`，足以固定“调度器 + 独立版本文件”的新增迁移样板；`014` 至 `047` 保持在既有 `runMigrations.ts`，不再机械逐版本迁移。
- 该收敛不改变生产库当前 `user_version=47`、迁移 SQL、迁移顺序、幂等检查、数据或公开行为。历史版本仍用于新建库和旧库升级；已运行的 v47 数据库只继续执行既有幂等检查。
- 从下一次真实数据库变更的 `048` 起，迁移必须新增独立版本文件并由 `runMigrations.ts` 调度；不得为降低行数重写或重排旧迁移。
- 理由：继续提取 34 个历史版本的维护收益低于反复提取、验证和部署引入的风险；治理优先转向启动装配等仍有稳定职责边界的文件。

### Phase 4 第 14 子批：启动装配边界

- 新增 `src/app/createRuntimeServerDeps.ts`，集中装配 HTTP 层需要的数据库读写、内容读模型、来源管理、公众号草稿推送、报表读取及认证回调；`main.ts` 只保留运行配置、数据库初始化、运行锁、任务调度、监听和优雅退出。
- 启动层仍显式传入手动任务、运行状态、环境变量与运行时配置；未修改路由 URL、鉴权、数据库 SQL、锁粒度、任务触发、图片目录或关闭顺序。
- 新增 `createRuntimeServerDeps.test.ts` 覆盖手动任务转接和图片目录约定；服务端类型检查、服务端路由定向测试与生产构建在本批收口时执行。
- 提交 `356e729` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T10-25-30-494Z.sqlite` 验证通过，部署后 `user_version=47`、`quick_check=ok`，服务为 `active` 且服务器本机健康检查返回 `{"ok":true}`。

### 品牌 Logo 静态资源优化（2026-08-01）

- 侧栏、登录页和 legacy 页面实际以 `34`–`56px` 展示 Logo，原资源为 `2048×2048`、约 `772KB` 的 PNG；公网无 Cookie 请求实测下载约 `790651B`、`1.33s`，且原响应未设置浏览器缓存策略。
- `hotnow-logo-mark.png` 已替换为视觉一致的 `128×128`、约 `24KB` PNG，页面统一改用 `?v=1` 的版本化 URL；无版本兼容直链缓存 `1` 天，版本化 URL 使用 `Cache-Control: public, max-age=31536000, immutable`。
- 服务端品牌路由测试覆盖两类缓存头和资源体积上限；不改鉴权、页面结构或品牌视觉。
- 提交 `e551946` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T10-35-58-605Z.sqlite` 验证通过。部署后无 Cookie 公网 GET 返回 `200`、`24655B`、`Cache-Control: public, max-age=31536000, immutable`，服务器本机健康检查正常。

### 技术债收口：登录回跳测试与日报 API 构建告警（2026-08-01）

- `createServer.test.ts` 的 4 个断言已改为匹配既有安全契约：匿名访问受保护页时跳转至携带原路径的 `/login?redirect=...`；未修改认证、跳转或权限行为，服务端路由测试恢复为 `25/25` 通过。
- 日报页面和详情抽屉已移除对 `dailyDigestApi.ts` 的重复动态导入，改为复用各自已经存在的静态导入；请求时机和接口不变，生产构建不再输出该模块“同时静态/动态导入”的告警。
- 提交 `190fa7c` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T10-46-05-613Z.sqlite` 验证通过，服务器本机与 `https://now.achuan.cc/health` 均返回 `{"ok":true}`。

### 技术债收口：统一壳层测试基线（2026-08-01）

- `appShell.test.ts` 已与当前已启用的壳层路由、品牌文案、页面/导航锚点和侧栏规格同步：AI 时间线及其后台入口自 2026-05-17 暂时下架，不再作为导航或懒加载路由断言；内容创作、系统配置和移动端可直达工作区入口均按当前路由表覆盖。
- 壳层挂载时默认内容页会请求符合 `ContentPageModel` 的分页模型；测试此前将该请求错误 mock 为 `{ ok: true }`，导致异步渲染访问 `cards` 时出现未处理异常。现改为最小类型正确的空态夹具。
- 未修改路由、导航、接口、鉴权或用户行为；本批仅恢复测试对现行产品契约的验证。
- 提交 `7eaf97c` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T10-55-05-088Z.sqlite` 验证通过，服务为 `active`，服务器本机与 `https://now.achuan.cc/health` 均返回 `{"ok":true}`。

### Phase 1 后续：来源工作台表单生命周期边界（2026-08-01）

- 新增 `useSourcesPageForms.ts`，集中六类来源配置的初始值、弹窗开关、编辑回填、关闭清理与提交前 payload 校验；`useSourcesPageController.ts` 保留加载、分钟级时间刷新、统一操作包装、通知和 API 调用。
- 页面暴露字段、请求时机、来源启停、手动采集、创建/编辑/删除行为和错误提示均保持不变；控制器仅通过一个明确的回调交给页面提示“不支持编辑”的普通来源。
- `sourcesPage.test.ts` 同步 AI 时间线摘要的既有短日期格式 `MM-DD HH:mm`；来源页定向测试 29 项与客户端类型检查通过。
- 提交 `e4babba` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T11-01-24-498Z.sqlite` 验证通过，服务为 `active`，服务器本机与 `https://now.achuan.cc/health` 均返回 `{"ok":true}`。

### 文档职责与 AI 时间线入口收敛（2026-08-01）

- 新增 `docs/开发与模块化规范.md` 作为当前模块边界、依赖方向、重构触发条件、验证规则和文档分工的唯一技术规范；当时协作约束、快速上手和工具专用补充分别收敛到对应入口。
- 本文明确回归为性能与治理历史记录，旧阶段的文件行数和进度不再被描述为当前事实；当前状态以代码、相关测试和长期规范为准。
- Vue 客户端已下架的 `/ai-timeline`、`/settings/ai-timeline` 不再出现在项目级入口、smoke 指引或来源页跳转中；feed、API 与 S 级提醒保持运行。
- 提交 `7bd9640` 已推送并部署；部署前生产库快照 `/srv/hot-now/shared/data/recovery-backups/pre-deploy-2026-08-01T11-14-49-657Z.sqlite` 验证通过，服务为 `active`，服务器本机与 `https://now.achuan.cc/health` 均返回 `{"ok":true}`。

### 协作文档单一入口（2026-08-01）

- 已将旧专用协作文档中仍有效的 TypeScript 模块解析与客户端构建约束并入 `AGENTS.md`，并删除该重复入口；当前协作约束只维护在 `AGENTS.md`。
- 历史计划目录已删除，不再作为当前规则或文件入口；如需追溯已完成批次的决策上下文，使用 Git 历史查看。

### 文档目录收敛（2026-08-01）

- 删除一次性图片提示词、历史反馈、已完成接口需求和重复的外部 Agent 文档；Git 历史保留追溯能力。
- 当前 `docs/` 只保留开发规范、性能治理历史、外部智能体接口和生产部署手册四类稳定职责文档。

### 账号适配自动化队列上线（2026-08-01）

- HotNow 提交 `efd8b75` 已部署。部署前生产 SQLite `quick_check` 与可恢复快照通过，生产依赖审计为 `0` 漏洞；服务重启后公网 `/health` 返回 `{"ok":true}`。
- 生产数据库已执行独立迁移 `048_creative_account_fit_automation`：`user_version=48`，版本记录存在，任务、开关和告警三张自动化表均已核验存在。
- 本机 Hermes API 已重载，`http://127.0.0.1:8766/health` 返回正常。未通过生产接口创建评估或写作任务，避免为冒烟检查消耗写作配额或触发实际生产。

### 账号适配自动补偿修正（2026-08-01）

- 生产核验发现 39 条最近 72 小时的长素材在自动化上线前已被采集器写为 `ready`，而原补偿扫描只选择 `pending`，因此未被入评估队列；这与“最近三天长素材都评估”的确认规则不一致。
- 补偿条件已扩大为最近 72 小时、未评估、未关联成品且状态为 `pending` 或 `ready` 的长素材；短内容、72 小时前素材及已有成品素材仍不进入自动评估。
- 新增回归测试覆盖 `ready` 素材入队和已关联成品素材跳过，避免再次出现上线窗口内素材漏评估。
